import { Container, Title } from "@mantine/core";

export default function Tryout() {
    return (<Container p={0} m={0} left={0}>
        <Title>Try AI-Scout out!</Title>
        test
    </Container>);
}